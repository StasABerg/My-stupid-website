name: terminal-ci-cd
run-name: "Dev Deploy - Terminal Service - ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.branch || github.ref_name }}"

on:
  push:
    branches: [ main ]
    paths:
      - 'terminal-service/**'
      - 'docker/terminal.Dockerfile'
      - 'charts/terminal-service/**'
      - '.forgejo/workflows/ssh-terminal.yaml'
  workflow_dispatch:
    inputs:
      branch:
        type: string
        description: 'Branch or ref to deploy'
        required: true
        default: main

permissions:
  contents: read
  packages: write

concurrency:
  group: terminal-ci-cd-${{ github.event_name == 'workflow_dispatch' && github.event.inputs.branch || github.ref_name }}
  cancel-in-progress: true

env:
  CHECKOUT_REF: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.branch || github.ref }}
  REGISTRY_HOST: forgejo.gitgud.zip
  REGISTRY_NAMESPACE: stasaberg
  TRIVY_IMAGE: aquasec/trivy:0.50.1
  TRIVY_CACHE_DIR: /tmp/trivy-cache

jobs:
  trivy-dockerfile:
    name: Trivy Dockerfile (terminal)
    runs-on: self-hosted
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ env.CHECKOUT_REF }}

      - name: Scan terminal Dockerfile
        id: trivy
        run: |
          set -euo pipefail
          mkdir -p "${TRIVY_CACHE_DIR}"
          docker run --rm \
            -v "${TRIVY_CACHE_DIR}:/root/.cache/" \
            -v "${GITHUB_WORKSPACE}:/workspace" \
            "${TRIVY_IMAGE}" config \
              --severity HIGH,CRITICAL \
              --format table \
              --output /workspace/trivy-terminal-dockerfile.txt \
              /workspace/docker/terminal.Dockerfile

      - name: Upload report artifact
        if: ${{ always() && hashFiles('trivy-terminal-dockerfile.txt') != '' }}
        uses: actions/upload-artifact@v3
        with:
          name: trivy-terminal-dockerfile
          path: trivy-terminal-dockerfile.txt

  trivy-helm:
    name: Trivy Helm/K8s Config (terminal)
    runs-on: self-hosted
    needs: trivy-dockerfile
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ env.CHECKOUT_REF }}

      - name: Scan Helm charts
        id: trivy
        run: |
          set -euo pipefail
          mkdir -p "${TRIVY_CACHE_DIR}"
          docker run --rm \
            -v "${TRIVY_CACHE_DIR}:/root/.cache/" \
            -v "${GITHUB_WORKSPACE}:/workspace" \
            "${TRIVY_IMAGE}" config \
              --severity HIGH,CRITICAL \
              --format table \
              --output /workspace/trivy-terminal-helm.txt \
              /workspace/charts/terminal-service

      - name: Upload report artifact
        if: ${{ always() && hashFiles('trivy-terminal-helm.txt') != '' }}
        uses: actions/upload-artifact@v3
        with:
          name: trivy-terminal-helm
          path: trivy-terminal-helm.txt

  build:
    runs-on: self-hosted
    needs: trivy-dockerfile
    outputs:
      sha: ${{ steps.meta.outputs.sha }}
      attestation: ${{ steps.provenance.outputs.attestation }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ env.CHECKOUT_REF }}

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY_HOST }}
          username: ${{ secrets.FORGEJO_REGISTRY_USERNAME }}
          password: ${{ secrets.FORGEJO_REGISTRY_PASSWORD }}

      - name: Record image tag
        id: meta
        run: echo "sha=${GITHUB_SHA}" >> "$GITHUB_OUTPUT"

      - name: Build & push terminal image
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/terminal.Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY_HOST }}/${{ env.REGISTRY_NAMESPACE }}/my-stupid-website-terminal:${{ steps.meta.outputs.sha }}
            ${{ env.REGISTRY_HOST }}/${{ env.REGISTRY_NAMESPACE }}/my-stupid-website-terminal:latest
          provenance: false
          cache-from: type=local,src=/home/runner/.cache/buildx/terminal-service
          cache-to: type=local,dest=/home/runner/.cache/buildx/terminal-service,mode=max

      - name: Generate provenance attestation
        id: provenance
        env:
          IMAGE_SHA: ${{ steps.meta.outputs.sha }}
          BUILD_DIGEST: ${{ steps.build.outputs.digest }}
          COMMIT_SHA: ${{ github.sha }}
          GIT_REF: ${{ github.ref }}
          WORKFLOW_NAME: ${{ github.workflow }}
          RUN_ID: ${{ github.run_id }}
        run: |
          ATTESTATION=$(cat << EOF
          {
            "image": "${REGISTRY_HOST}/${REGISTRY_NAMESPACE}/my-stupid-website-terminal:${IMAGE_SHA}",
            "digest": "${BUILD_DIGEST}",
            "builder": "forgejo-actions",
            "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "commit": "${COMMIT_SHA}",
            "ref": "${GIT_REF}",
            "workflow": "${WORKFLOW_NAME}",
            "run_id": "${RUN_ID}"
          }
          EOF
          )
          echo "${ATTESTATION}" > provenance.json
          echo "attestation<<EOF" >> $GITHUB_OUTPUT
          echo "${ATTESTATION}" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Upload provenance
        uses: actions/upload-artifact@v3
        with:
          name: provenance-terminal-service
          path: provenance.json

  trivy-image:
    name: Trivy Image (terminal)
    runs-on: self-hosted
    needs: build
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Scan built terminal image
        id: trivy
        env:
          IMAGE_SHA: ${{ needs.build.outputs.sha }}
        run: |
          set -euo pipefail
          mkdir -p "${TRIVY_CACHE_DIR}"
          IMAGE="${REGISTRY_HOST}/${REGISTRY_NAMESPACE}/my-stupid-website-terminal:${IMAGE_SHA}"
          docker run --rm \
            -v "${TRIVY_CACHE_DIR}:/root/.cache/" \
            -v "${GITHUB_WORKSPACE}:/workspace" \
            "${TRIVY_IMAGE}" image \
              --severity HIGH,CRITICAL \
              --ignore-unfixed \
              --format table \
              --output /workspace/trivy-terminal-image.txt \
              "${IMAGE}"

      - name: Upload report artifact
        if: ${{ always() && hashFiles('trivy-terminal-image.txt') != '' }}
        uses: actions/upload-artifact@v3
        with:
          name: trivy-terminal-image
          path: trivy-terminal-image.txt

  deploy-dev:
    runs-on: self-hosted
    needs: [trivy-dockerfile, build, trivy-image, trivy-helm]
    if: |
      needs.trivy-dockerfile.result == 'success' &&
      needs.build.result == 'success' &&
      needs.trivy-image.result == 'success' &&
      needs.trivy-helm.result == 'success'
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ env.CHECKOUT_REF }}

      - name: Configure Kubeconfig
        env:
          KUBECONFIG_BASE64: ${{ secrets.KUBECONFIG }}
        run: |
          echo "$KUBECONFIG_BASE64" | base64 -d > $HOME/kubeconfig
          chmod 600 $HOME/kubeconfig
          echo "KUBECONFIG=$HOME/kubeconfig" >> $GITHUB_ENV

      - name: Helm upgrade (dev - terminal)
        env:
          IMAGE_SHA: ${{ needs.build.outputs.sha }}
        run: |
          helm upgrade --install my-stupid-website-terminal charts/terminal-service \
            -f charts/terminal-service/values-dev.yaml \
            --namespace my-stupid-website-dev --create-namespace \
            --set-string image.tag="${IMAGE_SHA}" \
            --rollback-on-failure --wait --kube-insecure-skip-tls-verify

      - name: Output deployment info
        env:
          IMAGE_SHA: ${{ needs.build.outputs.sha }}
        run: |
          echo "✅ Deployed to development"
          echo "📦 Image: ${REGISTRY_HOST}/${REGISTRY_NAMESPACE}/my-stupid-website-terminal:${IMAGE_SHA}"
          echo "🏷️  Tag for production: ${IMAGE_SHA}"
