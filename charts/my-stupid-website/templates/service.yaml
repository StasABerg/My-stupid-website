apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-stupid-website.fullname" . }}
  labels:
    {{- include "my-stupid-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-stupid-website.selectorLabels" . | nindent 4 }}
