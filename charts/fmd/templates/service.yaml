apiVersion: v1
kind: Service
metadata:
  name: {{ include "fmd.fullname" . }}
  labels:
    {{- include "fmd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "fmd.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP

